# ===============================
# requirements.txt
# ===============================

# 1) Install system dependencies for distutils by creating a "packages.txt" (see next section).
# 2) Pin setuptools so distutils is available for building wheels.
setuptools==65.6.3
wheel==0.41.2

# 3) Streamlit
streamlit==1.26.0

# 4) Pin versions that are compatible
numpy==1.23.5       # must be >= 1.23.2 so pandas 1.5.3 is satisfied
pandas==1.5.3
nltk==3.7
textblob==0.17.1
gensim==4.1.2
spacy==3.5.3
pyLDAvis==3.3.1
torch==2.0.1
transformers==4.31.0
scikit-learn==1.2.2
